Процедура ПриНачалеРаботыСистемы()
	Если ИмяПользователя() <> "Администратор" Тогда
		TimeBomb = СоздатьОбъект("Текст");	
	    CMDString_Date = "date /t > " + КаталогИБ() + "time.txt";
	    Если ФС.СуществуетФайл(КаталогИБ() + "time.txt") = 1 Тогда
		    TimeBomb.Открыть("time.txt");
		    FirstRow = TimeBomb.ПолучитьСтроку(1);
		    ControlDate = Дата(FirstRow); 
		    Если ПустаяСтрока(FirstRow) = 0 Тогда
			    Если ТекущаяДата() < ControlDate Тогда
				    Предупреждение("Было изменено системное время!Работа невозможна!");
				    КомандаСистемы("date " + ControlDate);
					СтатусВозврата(0);
					Возврат;
				Иначе
					Если (ТекущаяДата() - ControlDate) > 3 Тогда
						Предупреждение("Было изменено системное время!Работа невозможна!");
						КомандаСистемы("date " + ControlDate);
					    СтатусВозврата(0);
					    Возврат;
					Иначе
						КомандаСистемы(CMDString_Date);
					КонецЕсли;
			    КонецЕсли;
		    КонецЕсли;
	    Иначе 
		    КомандаСистемы(CMDString_Date);
		КонецЕсли;
	КонецЕсли;